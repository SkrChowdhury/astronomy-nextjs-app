/** @type {import('next').NextConfig} */
const nextConfig = {
images: {
    domains: ['apod.nasa.gov', 'nasa.gov','www.youtube.com'], // Add any relevant NASA domains here
  },
};

export default nextConfig;
